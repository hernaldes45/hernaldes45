#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    double edge , volume ; 
    cout<<"Enter the edge value : ";
    cin>>edge;
    volume=pow(edge, 3);
    cout<<"the volume is :"<<volume;
    return 0;
}
